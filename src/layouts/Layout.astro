<html lang="es">
  <head>
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Metadata esencial -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="description" content="Limpieza premium de zapatillas deportivas y de lujo en Panamá. Deja tus zapatillas como nuevas con MyShus." />
    <meta name="keywords" content="limpieza zapatillas, cuidado de calzado, limpieza de lujo, Panamá, MyShus" />
    <meta name="author" content="MyShus" />

    <!-- Open Graph (para redes sociales) -->
    <meta property="og:title" content="MyShus - Limpieza Premium de Zapatillas" />
    <meta property="og:description" content="Limpieza profesional y especializada para todo tipo de zapatillas. Reserva ahora y disfruta del mejor servicio en Panamá." />
    <meta property="og:image" content="https://myshusla.com/images/preview-image.png" />
    <meta property="og:url" content="https://myshusla.com" />
    <meta property="og:type" content="website" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="MyShus - Limpieza Premium de Zapatillas" />
    <meta name="twitter:description" content="Tu servicio confiable de limpieza para zapatillas de lujo y deportivas." />
    <meta name="twitter:image" content="https://myshusla.com/images/preview-image.png" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/favicon.ico" />

    <!-- Fuente Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link 
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" 
      rel="stylesheet"
    />

    <!-- CSS Global -->
    <link rel="stylesheet" href="/styles/global.css" />

    <!-- Título -->
    <title>MyShus - Limpieza Profesional de Zapatillas</title>

    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-JESF85NFZ1"></script>
    <script>
      // Declare global dataLayer interface
      declare global {
        interface Window {
          dataLayer: any[];
        }
      }
    
      // Initialize the dataLayer
      window.dataLayer = window.dataLayer || [];
      function gtag(...args: any[]) {
        window.dataLayer.push(args);
      }
    
      gtag('js', new Date());
      gtag('config', 'G-JESF85NFZ1');
    </script>

    <!-- Canonical URL (SEO) -->
    <link rel="canonical" href="https://myshusla.com" />

    <!-- Preload important resources -->
    <link rel="preload" href="/images/hero-image.png" as="image" />
    <link rel="preload" href="/styles/global.css" as="style" />
  </head>

  <body>
    <slot />

    <!-- Lazy Load Images -->
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const lazyImages = document.querySelectorAll("img[data-src]");
        lazyImages.forEach(img => {
          const dataSrc = img.getAttribute("data-src") || "";
          img.setAttribute("src", dataSrc);
          img.removeAttribute("data-src");
        });
      });
    </script>
  </body>
</html>
