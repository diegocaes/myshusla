<section id="about" class="how-it-works">
    <h2 class="section-title">
      Lograr que tus zapatillas se vean increíblemente limpias, <span class="highlight">nunca había sido tan fácil</span>. Sigue estos tres pasos:
    </h2>
    <div class="steps-container">
      <!-- Step 1 -->
      <div class="step">
        <img src="/images/schedule-icon.png" alt="Agenda tu limpieza" class="step-icon" />
        <h3 class="step-title">Agenda tu limpieza</h3>
        <p class="step-description">
          Escríbenos y agenda una fecha para que recojamos tus zapatillas. Indícanos el tipo de limpieza que deseas y los servicios extras que quieras.
        </p>
      </div>
  
      <!-- Step 2 -->
      <div class="step">
        <img src="/images/hand-in-icon.png" alt="Recogemos tus zapatillas" class="step-icon" />
        <h3 class="step-title">Recogemos tus Zapatillas</h3>
        <p class="step-description">
          Empaca tus zapatillas y nosotros las recogeremos en la fecha acordada. El arte de la limpieza ocurre en nuestra tienda.
        </p>
      </div>
  
      <!-- Step 3 -->
      <div class="step">
        <img src="/images/receive-icon.png" alt="Recibe tus zapatillas relucientes" class="step-icon" />
        <h3 class="step-title">Recibe tus Zapatillas Relucientes</h3>
        <p class="step-description">
          Te notificaremos una vez las zapatillas estén listas y su fecha de entrega. Puedes recogerlas en nuestra tienda si lo deseas!
        </p>
      </div>
    </div>
  </section>
  
  <style>
    /* About Section Container */
    .how-it-works {
      text-align: center;
      padding: 3rem 2rem;
      background-color: #ebebeb; /* Light gray background for better contrast */
      border-top: 4px solid #25d366; /* Subtle green accent line */
      border-bottom: 4px solid #25d366;
    }
  
    /* Section Title */
    .section-title {
      font-family: 'Inter', sans-serif;
      font-size: 1.5rem;
      font-weight: 600;
      color: #000;
      margin-bottom: 2rem;
      line-height: 1.5;
    }
  
    .highlight {
      color: #25d366; /* Vibrant green for emphasis */
      font-weight: bold;
    }
  
    /* Steps Container */
    .steps-container {
      display: flex;
      justify-content: space-between;
      gap: 2rem;
      flex-wrap: wrap; /* Ensure responsiveness */
    }
  
    /* Individual Step */
    .step {
      flex: 1;
      max-width: 300px; /* Limit step width */
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 1.5rem;
      background: #fff; /* White card background */
      border: 1px solid #ddd;
      border-radius: 10px; /* Rounded corners for modern look */
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Subtle shadow */
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
  
    .step:hover {
      transform: translateY(-5px); /* Lift the step slightly */
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Stronger shadow on hover */
    }
  
    .step-icon {
      height: 80px; /* Icon size */
      margin-bottom: 1rem;
    }
  
    .step-title {
      font-family: 'Inter', sans-serif;
      font-size: 1.25rem;
      font-weight: 600;
      color: #000;
      margin-bottom: 1rem;
    }
  
    .step-description {
      font-family: 'Inter', sans-serif;
      font-size: 1rem;
      font-weight: 400;
      color: #555; /* Dark gray */
      line-height: 1.5;
    }
  
    /* Responsive Adjustments */
    @media (max-width: 768px) {
      .steps-container {
        flex-direction: column;
        gap: 2rem;
      }
    }
  </style>