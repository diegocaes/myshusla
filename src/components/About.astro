<section id="about" class="how-it-works">
  <div class="container">
    <h2 class="section-title">¿Cómo Funciona Nuestro Servicio a Domicilio?</h2>
    
    <div class="steps-container">
      <!-- Step 1 -->
      <div class="step">
        <div class="step-number">1</div>
        <div class="step-icon-container">
          <div class="step-emoji">📱</div>
        </div>
        <h3>Agenda por WhatsApp</h3>
        <p>Escríbenos al +507 6443-9110 con fotos de tus zapatillas. Te daremos un presupuesto inmediato y coordinaremos la recogida.</p>
        <div class="step-details">
          <span class="detail-item">• Respuesta en menos de 1 hora</span>
          <span class="detail-item">• Presupuesto sin compromiso</span>
        </div>
      </div>

      <!-- Step 2 -->
      <div class="step">
        <div class="step-number">2</div>
        <div class="step-icon-container">
          <div class="step-emoji">🚚</div>
        </div>
        <h3>Recogida GRATIS*</h3>
        <p>Pasamos por tu casa u oficina en el horario acordado. Revisamos tus zapatillas y confirmamos el servicio contigo.</p>
        <div class="step-details">
          <span class="detail-item">• Horarios flexibles</span>
          <span class="detail-item">• Áreas seleccionadas de Panamá</span>
        </div>
      </div>

      <!-- Step 3 -->
      <div class="step">
        <div class="step-number">3</div>
        <div class="step-icon-container">
          <div class="step-emoji">🧡</div>
        </div>
        <h3>Limpieza Profesional</h3>
        <p>Llevamos tus zapatillas a nuestro taller donde aplicamos técnicas especializadas y productos profesionales para cada material.</p>
        <div class="step-details">
          <span class="detail-item">• 4-7 días de proceso</span>
          <span class="detail-item">• Seguimiento por WhatsApp</span>
        </div>
      </div>

      <!-- Step 4 -->
      <div class="step">
        <div class="step-number">4</div>
        <div class="step-icon-container">
          <div class="step-emoji">🎆</div>
        </div>
        <h3>Entrega a Domicilio</h3>
        <p>Te devolvemos tus zapatillas limpias, desinfectadas y como nuevas. Pago al recibir el trabajo terminado.</p>
        <div class="step-details">
          <span class="detail-item">• Inspección antes del pago</span>
          <span class="detail-item">• Garantía de satisfacción</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .how-it-works {
    background-color: var(--background-white);
    border-top: 3px solid var(--primary-green);
    border-bottom: 3px solid var(--primary-green);
    padding: var(--spacing-xl) 0;
    width: 100%;
    font-family: var(--font-primary);
    position: relative;
    z-index: 1;
    margin-bottom: 80px;
  }

  .container {
    position: relative;
    z-index: 2;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-md);
    background-color: var(--background-white);
  }

  .section-title {
    text-align: center;
    font-size: 2rem;
    margin-bottom: var(--spacing-xl);
    color: var(--color-text);
    position: relative;
    display: inline-block;
    left: 50%;
    transform: translateX(-50%);
  }

  .section-title::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 0;
    width: 60px;
    height: 3px;
    background-color: var(--primary-green);
    left: 50%;
    transform: translateX(-50%);
  }

  .steps-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .step {
    background: var(--background-white);
    border-radius: var(--border-radius-lg);
    padding: var(--spacing-lg);
    text-align: left;
    box-shadow: var(--shadow-lg);
    transition: transform var(--transition-duration) ease, box-shadow var(--transition-duration) ease;
    position: relative;
    border-left: 4px solid var(--primary-green);
  }

  .step:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 12px rgba(37, 211, 102, 0.15);
  }

  .step-number {
    position: absolute;
    top: -15px;
    left: -15px;
    background: var(--primary-green);
    color: white;
    width: 35px;
    height: 35px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.2rem;
    box-shadow: 0 4px 15px rgba(37, 211, 102, 0.3);
  }

  .step-icon-container {
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    width: 80px;
    height: 80px;
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
  }

  .step-emoji {
    font-size: 2.5rem;
  }

  .step:hover .step-icon-container {
    background: linear-gradient(135deg, var(--primary-green) 0%, var(--primary-green-hover) 100%);
    transform: scale(1.1);
  }

  .step:hover .step-emoji {
    filter: grayscale(0) brightness(1.2);
  }

  .step h3 {
    margin: 0 0 var(--spacing-md);
    color: var(--color-primary);
    font-size: 1.4rem;
    font-family: var(--font-primary);
    font-weight: 700;
  }

  .step p {
    margin: 0 0 var(--spacing-md);
    color: var(--color-text-light);
    line-height: 1.6;
    font-size: 1rem;
  }

  .step-details {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .detail-item {
    font-size: 0.9rem;
    color: var(--primary-green);
    font-weight: 500;
  }

  @media (max-width: 768px) {
    .how-it-works {
      padding: 2rem 0;
      margin-bottom: 100px;
    }

    .container {
      padding: 0 1rem;
    }

    .section-title {
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .steps-container {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .step {
      padding: 1.5rem;
      text-align: center;
    }

    .step-icon-container {
      width: 70px;
      height: 70px;
      margin: 0 auto 1rem;
    }

    .step-emoji {
      font-size: 2rem;
    }

    .step-details {
      align-items: center;
    }

    .step h3 {
      font-size: 1.1rem;
      margin-bottom: 0.5rem;
    }

    .step p {
      font-size: 0.9rem;
      line-height: 1.3;
    }
  }

  @media (max-width: 480px) {
    .how-it-works {
      margin-bottom: 120px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .step, .step-icon-container {
      transition: none;
    }
    .step:hover {
      transform: none;
    }
  }
</style>