<section id="about" class="how-it-works">
  <div class="container">
    <h2 class="section-title">¿Cómo Funciona?</h2>
    
    <div class="steps-container">
      <!-- Step 1 -->
      <div class="step">
        <div class="step-icon-container">
          <img 
            src="/images/schedule-icon.png" 
            alt="Icono de calendario"
            class="step-icon"
            loading="lazy"
            width="40"
            height="40"
          />
        </div>
        <h3>Agenda tu servicio</h3>
        <p>Contáctanos por WhatsApp para programar la limpieza de tus zapatillas.</p>
      </div>

      <!-- Step 2 -->
      <div class="step">
        <div class="step-icon-container">
          <img 
            src="/images/hand-in-icon.png" 
            alt="Icono de recogida"
            class="step-icon"
            loading="lazy"
            width="40"
            height="40"
          />
        </div>
        <h3>Recogemos tus zapatillas</h3>
        <p>Retiramos tus zapatillas desde la comodidad de tu hogar.</p>
      </div>

      <!-- Step 3 -->
      <div class="step">
        <div class="step-icon-container">
          <img 
            src="/images/receive-icon.png" 
            alt="Icono de entrega"
            class="step-icon"
            loading="lazy"
            width="40"
            height="40"
          />
        </div>
        <h3>Entrega a domicilio</h3>
        <p>Te devolvemos tus zapatillas limpias y como nuevas.</p>
      </div>
    </div>
  </div>
</section>

<style>
  :root {
    --primary-green: #25d366;
    --primary-green-hover: #22c35e;
    --font-primary: 'Inter', sans-serif;
  }

  .how-it-works {
    background-color: #ffffff;
    border-top: 3px solid var(--primary-green);
    border-bottom: 3px solid var(--primary-green);
    padding: 3rem 0;
    width: 100%;
    font-family: var(--font-primary);
    position: relative;
    z-index: 1;
    margin-bottom: 80px;
  }

  .container {
    position: relative;
    z-index: 2;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    background-color: #ffffff;
  }

  .section-title {
    text-align: center;
    font-size: 2rem;
    margin-bottom: 3rem;
    color: #333;
    position: relative;
    display: inline-block;
    left: 50%;
    transform: translateX(-50%);
  }

  .section-title::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 0;
    width: 60px;
    height: 3px;
    background-color: var(--primary-green);
    left: 50%;
    transform: translateX(-50%);
  }

  .steps-container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .step {
    background: #ffffff;
    border-radius: 12px;
    padding: 1.5rem;
    text-align: center;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .step:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 12px rgba(37, 211, 102, 0.15);
  }

  .step-icon-container {
    background: #f8f8f8;
    width: 65px;
    height: 65px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.25rem;
    transition: background-color 0.3s ease;
  }

  .step:hover .step-icon-container {
    background-color: #e8f8ed;
  }

  .step h3 {
    margin: 0 0 0.75rem;
    color: var(--primary-green);
    font-size: 1.2rem;
    font-family: var(--font-primary);
    font-weight: 600;
  }

  .step p {
    margin: 0;
    color: #666;
    line-height: 1.4;
    font-size: 0.95rem;
  }

  @media (max-width: 768px) {
    .how-it-works {
      padding: 2rem 0;
      margin-bottom: 100px;
    }

    .container {
      padding: 0 1rem;
    }

    .section-title {
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .steps-container {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .step {
      padding: 1.25rem;
    }

    .step-icon-container {
      width: 55px;
      height: 55px;
      margin-bottom: 1rem;
    }

    .step-icon {
      width: 30px;
      height: 30px;
    }

    .step h3 {
      font-size: 1.1rem;
      margin-bottom: 0.5rem;
    }

    .step p {
      font-size: 0.9rem;
      line-height: 1.3;
    }
  }

  @media (max-width: 480px) {
    .how-it-works {
      margin-bottom: 120px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .step, .step-icon-container {
      transition: none;
    }
    .step:hover {
      transform: none;
    }
  }
</style>