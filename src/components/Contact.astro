<section id="contact" class="contact-section" aria-labelledby="contact-title">
  <div class="contact-container">
    <!-- Left: Logo -->
    <div class="contact-left">
      <a href="/" aria-label="Volver al inicio">
        <img
          src="/images/myshus-logo-light.png"
          alt="Logo de MyShus"
          class="contact-logo"
          loading="lazy"
          width="150"
          height="25"
        />
      </a>
    </div>

    <!-- Center: Links -->
    <div class="contact-links">
      <div class="links-grid">
        <nav class="contact-column" aria-labelledby="empresa-title">
          <h3 class="column-title" id="empresa-title">Empresa</h3>
          <ul>
            <li>
              <a href="/#servicios" class="hover-effect">Servicios</a>
            </li>
            <li>
              <a href="/refunds" class="hover-effect">Refunds</a>
            </li>
          </ul>
        </nav>

        <nav class="contact-column" aria-labelledby="soporte-title">
          <h3 class="column-title" id="soporte-title">Soporte</h3>
          <ul>
            <li>
              <a href="/contact-form" class="hover-effect">Contáctanos</a>
            </li>
            <li>
              <a
                href="/pickup-delivery"
                class="hover-effect"
                data-feature="envios"
              >
                Información de Recogida y Entrega
              </a>
            </li>
            <li>
              <a href="/terms" class="hover-effect" data-feature="terminos">
                Términos y Condiciones
              </a>
            </li>
          </ul>
        </nav>
      </div>
    </div>

    <!-- Right: Social Media -->
    <div class="contact-right">
      <h3 class="column-title" id="social-title">Síguenos</h3>
      <div class="social-icons" aria-labelledby="social-title">
        <a
          href="https://www.instagram.com/myshusla"
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Visita nuestro Instagram"
          class="social-btn"
        >
          <img
            src="/images/instagram-icon.png"
            alt="Instagram"
            width="28"
            height="28"
            loading="lazy"
          />
        </a>

        <a
          href="https://wa.me/+50764439110?text=Hola%21%20Llegué%20desde%20la%20web%20myshusla.com%20(footer)%20y%20quiero%20información%20sobre%20limpieza%20de%20zapatillas"
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Contáctanos por WhatsApp"
          class="social-btn"
          data-track="footer-whatsapp"
        >
          <img
            src="/images/whatsapp-icon.png"
            alt="WhatsApp"
            width="28"
            height="28"
            loading="lazy"
          />
        </a>
      </div>
    </div>
  </div>
</section>

<!-- Footer Profesional -->
<footer class="site-footer">
  <p>&copy; MyShus SA 2025. All rights reserved.</p>
</footer>


<style>
  /* Contact Section */
  .contact-section {
    font-family: var(--font-primary);
    background-color: var(--color-primary);
    color: var(--color-secondary);
    padding: var(--spacing-xl) var(--spacing-lg);
  }

  .contact-container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 2fr 1.3fr;
    gap: var(--spacing-lg);
    align-items: start;
  }

  .contact-left,
  .contact-right {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .contact-right {
    margin-left: 2rem; /* Ajusta según el espacio que quieras */
  }

  .contact-logo {
    max-width: 150px;
    height: auto;
    transition: transform var(--transition-duration) ease;
  }

  .contact-logo:hover {
    transform: scale(1.05);
  }

  .links-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-lg);
  }

  .contact-column ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .contact-column li {
    margin-bottom: var(--spacing-md);
  }

  .column-title {
    margin-bottom: var(--spacing-md);
    font-weight: 600;
    margin-top: 0;
  }

  .hover-effect {
    position: relative;
    text-decoration: none;
    color: var(--color-secondary);
    transition: color var(--transition-duration) ease;
  }

  .hover-effect::after {
    content: "";
    position: absolute;
    width: 0;
    height: 2px;
    bottom: -2px;
    left: 0;
    background-color: var(--color-accent);
    transition: width var(--transition-duration) ease;
  }

  .hover-effect:hover::after {
    width: 100%;
  }

  .social-icons {
    display: flex;
    gap: var(--spacing-md);
    margin-top: var(--spacing-sm);
  }

  .social-btn {
    display: inline-flex;
    padding: var(--spacing-sm);
    border-radius: 50%;
    transition: transform var(--transition-duration) ease;
  }

  .social-btn:hover {
    transform: translateY(-2px);
  }

  /* Footer */
  .site-footer {
    background-color: var(--color-primary);
    color: var(--color-secondary);
    text-align: center;
    padding: var(--spacing-md);
    font-size: 0.875rem;
    font-family: var(--font-primary);
  }

  /* Media Queries */
  @media (max-width: 768px) {
    .contact-container {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
    }

    .contact-left,
    .contact-right,
    .contact-links {
      align-items: center;
      text-align: center;
    }

    .contact-right {
      margin-left: 0;
    }

    .links-grid {
      width: 100%;
      max-width: 500px;
      margin: 0 auto;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--spacing-xl) var(--spacing-lg);
    }

    .contact-column {
      text-align: left;
    }
  }

  @media (max-width: 480px) {
    .contact-section {
      padding: var(--spacing-lg) var(--spacing-md);
    }

    .links-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-lg);
    }

    .contact-column {
      text-align: center;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hover-effect::after,
    .contact-logo,
    .social-btn,
    .toast {
      transition: none;
    }
  }
</style>
