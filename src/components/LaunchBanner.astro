<aside class="launch-banner" role="banner" aria-label="Promoción especial">
  <p>Recogida y entrega gratis!*</p>
</aside>

<style>
  .launch-banner {
    background: linear-gradient(90deg, #28a745, #2ecc71);
    color: #fff;
    text-align: center;
    padding: 0.5rem;
    font-size: 1.2rem;
    font-weight: 700;
    border-radius: 5px;
    margin: 0.2rem 0;
    animation: fadeIn 1s ease-out;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  }

  .launch-banner p {
    margin: 0;
    padding: 0;
    line-height: 1.4;
    text-transform: uppercase;
    font-family: "Inter", sans-serif;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .launch-banner {
      font-size: 1rem;
      padding: 0.4rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .launch-banner {
      animation: none;
    }
  }
</style>