{
  "name": "myshus-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && git add dist && git commit -m 'Deploy build' && git subtree push --prefix dist origin main1"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@vercel/analytics": "^1.5.0",
    "astro": "^5.5.4",
    "hast-util-to-html": "^9.0.4",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "rollup": "^4.29.2",
    "vite": "^6.2.2"
  }
}
